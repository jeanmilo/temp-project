/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  distDir: "imherelookhere", //this line will tell the build to create a file with this name
};

export default nextConfig;
